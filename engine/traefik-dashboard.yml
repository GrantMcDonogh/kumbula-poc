http:
  routers:
    dashboard-app:
      rule: "Host(`dashboard.kumbula.local`)"
      service: dashboard-app
      entryPoints:
        - web
  services:
    dashboard-app:
      loadBalancer:
        servers:
          - url: "http://172.17.0.1:9000"
